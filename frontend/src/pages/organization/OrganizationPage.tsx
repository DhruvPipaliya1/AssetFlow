import { PlaceholderPage } from '../../components/common';

export default function OrganizationPage() {
  return (
    <PlaceholderPage
      title="Organization Setup"
      description="Departments, categories & employee directory — Admin only (F4)"
    />
  );
}
