import { PlaceholderPage } from '../../components/common';

export default function NotificationsPage() {
  return <PlaceholderPage title="Notifications" description="Activity feed & live alerts (F11)" />;
}
